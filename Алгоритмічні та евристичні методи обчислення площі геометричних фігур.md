### Алгоритмічні та евристичні методи обчислення площі геометричних фігур

#### Мета роботи

1. Закріпити знання про геометричне представлення полігонів у пам'яті комп'ютера.  
2. Реалізувати точний (аналітичний) та наближений (імовірнісний) методи обчислення площі.  
3. Провести порівняльний аналіз алгоритмів: Метод Гауса (Shoelace formula) vs Метод Монте-Карло.  
4. Дослідити залежність точності та швидкодії від складності полігону (кількості вершин) та кількості ітерацій.

#### Теоретичні відомості

Для обчислення площі полігону, заданого координатами вершин

[![][image1]](https://www.codecogs.com/eqnedit.php?latex=P%20%3D%20%5C%7B\(x_0%2C%20y_0\)%2C%20\(x_1%2C%20y_1\)%2C%20...%2C%20\(x_n%2C%20y_n\)%5C%7D#0),

використовуються різні підходи.

##### А. Бібліотечний метод (Shapely)

Бібліотека Shapely використовує оптимізовані реалізації геометричних алгоритмів (на базі GEOS C++). Це значення ми будемо використовувати як еталонне (Ground Truth) для розрахунку похибок.

##### Б. Метод Гауса (Формула шнурків / Shoelace formula)

Це аналітичний метод обчислення площі простого багатокутника. Площа дорівнює половині абсолютної суми різниць добутків координат сусідніх вершин:

[![][image2]](https://www.codecogs.com/eqnedit.php?latex=S%20%3D%20%5Cfrac%7B1%7D%7B2%7D%20%7C%20%5Csum_%7Bi%3D0%7D%5E%7Bn-1%7D%20\(x_i%20y_%7Bi%2B1%7D%20-%20x_%7Bi%2B1%7D%20y_i\)%20%7C#0)

(де остання точка [![][image3]](https://www.codecogs.com/eqnedit.php?latex=n#0) збігається з нульовою [![][image4]](https://www.codecogs.com/eqnedit.php?latex=0#0)).

##### В. Метод Монте-Карло

Імовірнісний метод, що базується на генерації випадкових точок у відомій області (Bounding Box), що описує полігон.

1. Визначаємо прямокутник, що охоплює полігон ([![][image5]](https://www.codecogs.com/eqnedit.php?latex=x_%7Bmin%7D%2C%20y_%7Bmin%7D%2C%20x_%7Bmax%7D%2C%20y_%7Bmax%7D#0)).  
2. Генеруємо [![][image6]](https://www.codecogs.com/eqnedit.php?latex=M#0) випадкових точок всередині прямокутника.  
3. Рахуємо кількість точок [![][image7]](https://www.codecogs.com/eqnedit.php?latex=K#0), що потрапили всередину полігону.  
4. Площа обчислюється як: 

   

   [![][image8]](https://www.codecogs.com/eqnedit.php?latex=S_%7Bpoly%7D%20%5Capprox%20S_%7Bbox%7D%20%5Ctimes%20%5Cfrac%7BK%7D%7BM%7D#0)

#### Завдання до виконання

##### 1\. Підготовка та генерація

1. Створіть проект та налаштуйте віртуальне оточення. Встановіть необхідні бібліотеки (shapely, matplotlib, numpy).  
2. Використати код з Додатку А для створення модуля генерації полігонів.  
3. Згенерувати та візуалізувати (зберегти зображення) три тестові полігони з різною кількістю вершин: N \= 10, N \= 50, N \= 100\.

##### 2\. Реалізація алгоритмів

1. Написати власну функцію обчислення площі за Методом Гауса.  
2. Написати власну функцію обчислення площі методом Монте-Карло.  
   * Примітка: Для перевірки входження точки в полігон у методі Монте-Карло дозволяється використовувати метод .contains() бібліотеки Shapely.

##### 3\. Дослідження точності Монте-Карло

Для полігону з N=50 вершин провести серію експериментів:

1. Змінювати кількість точок генерації ([![][image9]](https://www.codecogs.com/eqnedit.php?latex=M#0)): [![][image10]](https://www.codecogs.com/eqnedit.php?latex=100%2C%201000%2C%2010%20000%2C%20100%20000#0).  
2. Для кожного [![][image11]](https://www.codecogs.com/eqnedit.php?latex=M#0) обчислити відносну похибку у відсотках відносно еталонної площі (Shapely):  
     
   [![][image12]](https://www.codecogs.com/eqnedit.php?latex=%5Cdelta%20%3D%20%5Cfrac%7B%7CS_%7BMC%7D%20-%20S_%7BShapely%7D%7C%7D%7BS_%7BShapely%7D%7D%20%5Ctimes%20100%5C%25#0)

3. Побудувати графік залежності похибки від кількості точок [![][image13]](https://www.codecogs.com/eqnedit.php?latex=M#0).

##### 4\. Аналіз продуктивності (Benchmark)

1. Виміряти час виконання обчислень для всіх трьох методів (Shapely, Гаус, Монте-Карло з [![][image14]](https://www.codecogs.com/eqnedit.php?latex=M%3D10%5E5#0)) на полігонах різної складності (10, 50, 100, 1000 вершин).  
2. Використати модуль time або timeit.  
3. Результати занести в зведену таблицю.

### 

#### 4\. Вимоги до звіту (GitHub Repository)
Звіт здається у вигляді публічного репозиторію на GitHub.
Структура репозиторію:

/lab-polygon-area
│
├── /src                   # Вихідний код проекту
│   ├── main.py            # Основний скрипт запуску тестів
│   ├── generators.py      # Код генерації полігону (з Додатку А)
│   └── algorithms.py      # Ваша реалізація методів (Гаус, Монте-Карло)
│
├── /images                # Збережені графіки та візуалізації
│   ├── polygon_example.png
│   ├── error_plot.png
│   └── time_benchmark.png
│
├── requirements.txt       # Список залежностей
├── .gitignore             # Ігнорування (.venv, .idea, __pycache__)
└── README.md              # ПРОТОКОЛ РОБОТИ (ЗВІТ)


Вимоги до файлу README.md:  
Файл має бути оформлений з використанням розмітки Markdown і містити:

1. Опис: Коротко про суть реалізованих методів.  
2. Результати:  
   * Таблиця з результатами вимірювання часу.  
   * Графік збіжності методу Монте-Карло (вісь X \- ітерації, вісь Y \- похибка).  
   * Приклади згенерованих полігонів.  
3. Висновки:  
   * Порівняння швидкості методів (Гаус vs Монте-Карло).  
   * Аналіз точності Монте-Карло: скільки ітерацій достатньо для прийнятної точності?

### 

#### Додаток А: Код генератора полігону (generators.py)

Використайте цей код для створення файлу generators.py.
import math
import random
import matplotlib.pyplot as plt
from shapely.geometry import Polygon

def generate_polygon(num_points: int, radius: float = 10.0, irregularity: float = 0.35) -> Polygon:
    """
    Генерує випадковий полігон без самоперетинів.
    
    Алгоритм:
    1. Генеруються випадкові кути (від 0 до 2pi).
    2. Кути сортуються.
    3. Для кожного кута генерується випадковий радіус.
    4. Полярні координати перетворюються в Декартові (x, y).
    
    Args:
        num_points (int): Кількість вершин.
        radius (float): Середній радіус описуючого кола.
        irregularity (float): Коефіцієнт "випадковості" радіуса (0.0 - ідеальне коло, 1.0 - сильний розкид).
    
    Returns:
        Polygon: Об'єкт полігону Shapely.
    """
    
    # Генеруємо відсортовані кути
    angles = sorted([random.uniform(0, 2 * math.pi) for _ in range(num_points)])
    
    points = []
    for angle in angles:
        # Випадковий радіус для цієї вершини: r = radius * (1 ± irregularity)
        r = radius * (1 - irregularity * random.random())
        
        x = r * math.cos(angle)
        y = r * math.sin(angle)
        points.append((x, y))
        
    poly = Polygon(points)
    
    # Перевірка на валідність (рідкісні випадки топологічних помилок)
    if not poly.is_valid:
        return poly.buffer(0) 
        
    return poly

def visualize_polygon(polygon: Polygon, filename: str = None):
    """
    Візуалізує полігон. Якщо вказано filename, зберігає у файл.
    """
    x, y = polygon.exterior.xy
    
    plt.figure(figsize=(6, 6))
    plt.plot(x, y, color='blue', linewidth=2, zorder=1)
    plt.fill(x, y, color='skyblue', alpha=0.3)
    plt.scatter(x, y, color='red', s=20, zorder=2) # Вершини
    
    plt.title(f"Polygon (Vertices: {len(polygon.exterior.coords)-1})")
    plt.grid(True, linestyle='--', alpha=0.6)
    plt.axis('equal')
    
    if filename:
        plt.savefig(filename)
        print(f"Зображення збережено: {filename}")
        plt.close()
    else:
        plt.show()

# --- Тестовий запуск ---
if __name__ == "__main__":
    random.seed(42) # Фіксуємо seed для відтворюваності
    try:
        my_poly = generate_polygon(num_points=15, radius=50)
        print(f"Test Polygon Area (Shapely): {my_poly.area}")
        visualize_polygon(my_poly)
    except Exception as e:
        print(f"Error: {e}")